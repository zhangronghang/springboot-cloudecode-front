## 1. 仓库清理与变更隔离

- [x] 1.1 在 `.gitignore` 中加入 `node_modules/`、`dist/`、`.superpowers/` 及 TypeScript/Vite 临时输出规则，并用 `git check-ignore -v` 验证各类样例路径均命中预期规则
- [x] 1.2 仅从 Git 索引移除已跟踪的生成物且保留本地文件，随后用 `git status --short` 和文件存在性检查确认没有删除本地依赖、构建结果或覆盖无关修改
- [x] 1.3 将仓库清理作为独立本地提交完成，并用 `git show --stat --oneline HEAD` 确认该提交不包含业务源码或用户现有地图/样式改动

## 2. 后端契约与 API 客户端

- [x] 2.1 先补充 `imageTypes` 类型测试，再定义公开图片、足迹 `coverImage`/`imageCount` 和批量删除结果类型，并用 `npm test -- src/api/imageTypes.test.ts` 验证编译期样例与运行断言通过
- [x] 2.2 先扩展 `imageApi` 测试覆盖图片分页、新增一张和去重批量删除的 URL、方法、请求体与响应，再实现三个客户端方法，并用 `npm test -- src/api/imageApi.test.ts` 验证通过
- [x] 2.3 先增加更新接口不得包含文件或行政区字段的回归测试，再移除旧详情/Base64 方法和元数据更新的文件能力，并用 `npm test -- src/api/imageApi.test.ts` 验证创建与更新 multipart 契约均通过
- [x] 2.4 为 JPEG/PNG 与 50 MiB 上限添加共享文件校验及边界测试，并用 `npm test -- src/memories/memoryForm.test.ts` 验证非法类型、超限、恰好上限和合法文件行为

## 3. 足迹列表数据迁移

- [x] 3.1 先改写 loader 测试以要求使用列表中的封面和图片数量且不调用详情接口，再更新 `CityMemory` 映射，并用 `npm test -- src/memories/cityMemoryLoader.test.ts` 验证无 N+1 请求、空封面及无效行政区/日期过滤
- [x] 3.2 更新面板状态测试以覆盖图片变更后加载第 1 页和删除末页记录后的上一页回退，并用 `npm test -- src/memories/memoryPanelState.test.ts` 验证分页刷新规则

## 4. 图片集状态控制器

- [x] 4.1 先创建失败测试覆盖打开足迹、每页 12 张、空图片、加载失败、过期请求丢弃和翻页清空选择，再实现 `createImageGalleryState` 的浏览/管理基础状态，并用对应 Vitest 文件验证通过
- [x] 4.2 先增加同页与跨页上一张/下一张、原图失败重试和返回网格测试，再实现基于稳定 `imageId` 的预览导航，并用对应 Vitest 文件验证边界导航通过
- [x] 4.3 先增加上传成功、50 张禁用、普通 API 失败及网络结果不确定测试，再实现单文件上传、忙碌锁和刷新通知，并用对应 Vitest 文件验证不会自动重试且会触发权威数据刷新
- [x] 4.4 先增加图片 ID 去重、部分忽略、当前页回退及删除最后一张测试，再实现批量删除流程，并用对应 Vitest 文件验证删除数/忽略数反馈和空管理状态

## 5. 图片集弹窗组件

- [x] 5.1 先编写组件测试覆盖默认纯浏览网格、总数/页码、空状态、缩略图失败占位和显式进入/退出管理，再实现 `FootprintGalleryModal.vue`，并用 `npm test -- src/components/FootprintGalleryModal.test.ts` 验证通过
- [x] 5.2 先编写组件测试覆盖原图预览、上一张/下一张、返回网格、重试和 `window.open(originalUrl)`，再实现预览界面，并用该组件测试文件验证全部交互
- [x] 5.3 先编写组件测试覆盖单文件选择、文件名/上传中状态、删除确认、部分删除反馈与 50 张提示，再实现管理界面，并用该组件测试文件验证上传和删除文案清晰区分
- [x] 5.4 先编写可访问性测试覆盖初始焦点、Tab 循环、Esc/遮罩/关闭按钮、忙碌期关闭锁、滚动锁及焦点恢复，再实现 Teleport 弹窗生命周期，并用该组件测试文件验证清理逻辑
- [x] 5.5 添加仅作用于弹窗的 scoped 响应式样式，验证桌面 4×3 网格、小屏布局、原图等比完整显示及所有按钮可见，且运行 `npm run build` 确认样式与模板编译通过

## 6. 足迹面板集成

- [x] 6.1 先更新 `CityMemoryPanel` 测试覆盖封面 URL、图片数量、空/失败封面均可打开弹窗以及市级/区县级均可管理，再集成图片集弹窗，并用 `npm test -- src/components/CityMemoryPanel.test.ts` 验证通过
- [x] 6.2 先增加创建与编辑表单回归测试，再将文件输入限制为仅创建时显示并复用 JPEG/PNG、50 MiB 校验，确保更新请求没有文件或行政区字段，并用面板和表单测试验证通过
- [x] 6.3 先增加“删除足迹”独立确认测试，再实现包含标题、图片数量和不可恢复提示的确认界面，并用面板测试验证取消、失败保留及末页回退行为
- [x] 6.4 实现图片新增/删除后的面板第 1 页刷新与成功/不确定结果提示，并用面板测试验证卡片封面、数量和 `uploadTime` 排序上下文不会使用陈旧状态

## 7. 综合验证与发布检查

- [x] 7.1 运行 `npm test` 并确认全部 API、状态和组件测试通过且无既有测试回归
- [x] 7.2 运行 `npm run build` 并确认 TypeScript 检查及 Vite 生产构建成功，随后用 `git status --short` 确认未重新跟踪任何生成物或覆盖无关工作区修改
- [x] 7.3 在已升级后端的本地联调环境手工验证市级与区县级的列表、12 张分页、跨页预览、新窗口原图、添加、批量幂等删除、删除至空和删除整条足迹，并记录每个场景的结果
  - 联调记录（2026-09-01，本地 Vite `127.0.0.1:52403` → 后端 `127.0.0.1:8081`）：区县视图创建 13 张图片的足迹后，第 1 页显示 12 张、第 2 页显示 1 张，从第 12 张“下一张”成功进入第 13 张，原图在新窗口返回 200；批量提交 12 个有效 ID、1 个重复 ID 和 1 个不存在 ID 后返回 `requestedCount=13`、`deletedCount=12`、忽略 1 个、剩余 1 张；删除最后一张后足迹仍存在且 `imageCount=0`、`coverImage=null`，随后整条足迹删除成功。广州市级列表中的天河区足迹可进入同一管理弹窗。全部 `codex-smoke` 联调数据已清理。
- [ ] 7.4 在生产配置检查同源 `/api` 对 JSON 与缩略图/原图资源的转发规则，验证前端未拼接独立 API 主机且无需客户端鉴权配置
  - 当前证据：开发同源代理下 JSON、JPEG 缩略图和 JPEG 原图均返回 200；业务客户端仅使用相对 `/api`，图片直接使用后端相对 URL，未配置独立 API 主机或客户端鉴权。仓库未提供 Nginx、Ingress、Gateway 或其他生产部署清单，尚无法核验生产转发规则本身。
