## 1. 图片服务契约迁移

- [x] 1.1 先更新图片 API 客户端测试，覆盖五个 `/api/information/*` 路径、列表 JSON 条件、普通区县与直辖市区县上传字段，以及更新请求不含行政区字段；运行对应 Vitest 文件确认新断言先失败。
- [x] 1.2 更新图片 API 类型和客户端实现，加入结构化省、市、区县及 `uploadTime` 字段并适配最新请求格式；运行图片 API 与 Vite 代理测试确认通过。

## 2. 行政区上下文与领域映射

- [x] 2.1 为市级和区县级判别上下文补充单元测试，验证查询条件、操作能力、普通区县上传字段及直辖市省略 `cityCode`；运行上下文测试确认新断言先失败。
- [x] 2.2 实现能力驱动的行政区上下文和请求参数构造，并移除位置归属标签生成逻辑；运行上下文与标签测试确认只保留游玩日期和用户标签。
- [x] 2.3 更新足迹加载器测试，覆盖按 `cityCode`、`districtCode` 查询、区县名称解析、无效归属或日期过滤及单图详情失败降级；实现映射后运行加载器测试确认通过。

## 3. 信息面板行为

- [x] 3.1 先扩展面板状态测试，覆盖删除当前页最后一条后回退上一页、删除失败保留记录和列表失败可重试；实现状态调整后运行状态测试确认通过。
- [x] 3.2 先扩展信息面板组件测试，验证市级隐藏创建入口但保留编辑删除、区县显示完整增删改、编辑不提交行政区字段及写失败保留界面状态；更新通用面板后运行组件测试确认通过。
- [x] 3.3 在市级信息卡片展示可识别的所属区县名称，并通过组件测试验证未知区县记录不会显示。

## 4. 页面与地图集成

- [x] 4.1 更新普通省份和市级页面的数据流，使省级页面不创建信息上下文、市级页面按 `cityCode` 查询、选择区县后按 `districtCode` 查询；补充页面测试验证对应请求和操作入口。
- [x] 4.2 更新省级地图选择规则和直辖市页面状态，使北京、上海、天津、重庆区县可通过鼠标与键盘直接进入区县信息面板且不请求额外县级地图；运行地图与页面测试确认普通省份导航不回归。
- [x] 4.3 验证离开或取消直辖市区县选择会清除当前信息上下文，且未选择区县时不调用信息接口。

## 5. 综合验证

- [x] 5.1 运行完整 `npm test`，确认接口、行政区上下文、标签、加载器、面板、分页和地图测试全部通过。
- [x] 5.2 运行 `npm run build`，确认 TypeScript 类型检查和 Vite 生产构建成功。
- [x] 5.3 对照 `http://localhost:8081/v2/api-docs` 手动核对上传三级行政区字段均为可选，并在普通省份城市、普通区县和直辖市区县各完成一次页面交互验收。
