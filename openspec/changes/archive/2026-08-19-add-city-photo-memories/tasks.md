## 1. 基础设施与领域模型

- [x] 1.1 在 Vite 开发配置中添加 `/api` 到图片服务的代理，并验证 Swagger 接口可经代理访问。
- [x] 1.2 定义图片服务响应、图片元数据、分页结果和城市足迹的 TypeScript 类型。
- [x] 1.3 先编写受管理标签生成、解析和校验的单元测试，覆盖城市编码、日期及用户标签的隔离。
- [x] 1.4 实现受管理标签工具，将图片元数据映射为城市足迹领域对象。

## 2. 图片服务客户端

- [x] 2.1 先编写图片服务客户端的失败和请求参数测试，覆盖 JSON、multipart、HTTP 状态与业务状态码。
- [x] 2.2 实现图片列表、详情、上传、更新和删除请求封装。
- [x] 2.3 实现按城市标签查询、按需加载图片 Base64 内容及单条图片加载失败的降级处理。

## 3. 城市足迹界面

- [x] 3.1 先编写城市足迹区域的页面状态测试，覆盖加载、空数据、失败重试和分页。
- [x] 3.2 在市级行政区页面新增城市足迹列表，并保留现有区县地图与导航行为。
- [x] 3.3 实现创建与编辑表单，要求图片、标题和游玩日期，并支持替换图片。
- [x] 3.4 实现删除确认、提交中状态、成功刷新和失败提示。
- [x] 3.5 补充响应式样式、键盘可访问性和图片加载占位状态。

## 4. 验证

- [x] 4.1 运行全部单元测试并修复回归。
- [x] 4.2 运行类型检查与生产构建。
- [x] 4.3 在本地图片服务上手工验证创建、查看、编辑（含替换图片）、删除及城市隔离。

## 5. 区县独立足迹

- [x] 5.1 先编写市级与区县级受管理标签及隔离查询的失败测试。
- [x] 5.2 实现层级行政区标签解析、生成与查询。
- [x] 5.3 先编写区县边界选择后切换足迹标题和列表的失败测试。
- [x] 5.4 实现区县边界选择状态及足迹面板的上下文切换。
- [x] 5.5 在本地服务验证同一城市与区县的足迹相互隔离。
