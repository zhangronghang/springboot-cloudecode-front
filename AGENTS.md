- 本文件及其后续新增、修改的规则内容必须使用中文描述；命令、文件名、代码标识符和专有工具名称可保留原文。

## OpenSpec 与 Superpowers 工作流

- 对于范围小且目标明确的改动（文案、样式、局部缺陷修复，或清晰的单行配置变更），不强制使用 OpenSpec。应遵循适用的 Superpowers 调试、测试与验证工作流。

- 对于中大型改动（新增功能、跨模块变更、接口、权限或数据模型变更，以及重构），按以下顺序执行：
  1. 若需求或实现路径尚不明确，使用 OpenSpec `explore` 调研现有代码库及其约束。
  2. 若产品或技术取舍需要决策，使用 Superpowers `brainstorming` 与用户收敛设计方案。
  3. 设计方案获批后，使用 OpenSpec `propose` 创建并评审提案、规格增量和任务。
  4. 实施期间遵循适用的 Superpowers 工作流，包括测试驱动开发、系统化调试、代码审查与验证。
  5. 验收完成后，使用 OpenSpec `archive` 归档本次已完成的变更。

- 避免生成重复的规划产物：
  - OpenSpec `explore` 用于了解现状与约束。
  - Superpowers `brainstorming` 用于作出设计决策。
  - 将已确认的需求和决策记录在 OpenSpec 产物中；除非变更复杂到确有必要，否则不要额外创建独立的实施计划。
